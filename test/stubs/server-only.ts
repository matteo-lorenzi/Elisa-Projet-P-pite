// Stub de `server-only` pour vitest : le vrai paquet throw hors contexte
// React Server Component (condition `react-server` absente sous Node). En test
// on neutralise la garde — elle reste active au build Next.
export {};
